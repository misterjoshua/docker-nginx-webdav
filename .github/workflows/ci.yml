name: CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build and test the docker image
      run: |
        DOCKER_IMAGE=docker-nginx-webdav:latest \
        ./docker-build.sh